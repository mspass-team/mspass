FROM --platform=linux/arm64 continuumio/anaconda3:latest
COPY . /mspass
WORKDIR /mspass
RUN ls
RUN pwd
ENV PYTHON_VERSION=3.11
RUN apt-get update 
RUN apt-cache madison cmake
RUN apt-get install -y cmake=3.13.4-1 \
    && sed -i '38d' meta.yaml \
    && conda install conda-build anaconda-client \
    && conda config --set anaconda_upload yes \
    && conda config --add channels conda-forge \
    && export ANACONDA_API_TOKEN=cx-8248b24a-767b-41de-ab7d-446cdc390b23 \
    && conda build . --python 3.11
